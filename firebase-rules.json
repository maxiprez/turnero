{
  "rules": {
    "salasaurio": {
      "config": {
        ".read": "auth != null",
        ".write": "auth != null"
      },
      "reservas": {
        ".read": "auth != null",
        "$date": {
          "$time": {
            ".write": "auth != null"
          }
        }
      }
    }
  }
}
